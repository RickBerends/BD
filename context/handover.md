# Handover

Snelle status voor wie dit oppakt tussen branches door.

- `master` en `claude/fetch-origin-main-hz42hu` staan na deze push weer gelijk.
- **Openstaand:** Cloudflare Web Analytics is ingebouwd
  (`src/layouts/BaseLayout.astro`) maar heeft nog een placeholder-token
  (`REPLACE_WITH_CLOUDFLARE_SITE_TOKEN`) — vervang die door een echt site-token uit
  het Cloudflare-dashboard, anders komt er geen data binnen.
- Zie `context/roadmap.md` voor de volledige backlog en `context/gdpr-avg-check.md`
  voor de privacy-status — niet hier gedupliceerd.
